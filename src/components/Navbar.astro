<nav class="navbar">
  <a class="left" href="/">My Notes</a>

  <button class="menu-btn" id="menuBtn">☰</button>

  <div class="right">
    <a href="/">Home</a>
    <a href="/about">About</a>
    <a href="/project">Project</a>
  </div>
</nav>

<script is:inline>
  const btn = document.getElementById("menuBtn");
  const sidebar = () => document.querySelector(".sidebar");
  const overlay = () => document.querySelector(".overlay");
  const mobileMenu = () => document.querySelector(".mobile-menu");

  const toggle = () => {
    sidebar()?.classList.toggle("open");
    overlay()?.classList.toggle("show");
    mobileMenu()?.classList.toggle("show");
  };

  const close = () => {
    sidebar()?.classList.remove("open");
    overlay()?.classList.remove("show");
    mobileMenu()?.classList.remove("show");
  };

  const isOpen = () =>
    sidebar()?.classList.contains("open") ||
    mobileMenu()?.classList.contains("show");

  btn?.addEventListener("click", (event) => {
    event.stopPropagation();
    toggle();
  });

  document.addEventListener("click", (event) => {
    const target = event.target;

    if (!isOpen() || btn?.contains(target)) return;

    close();
  });

  document.addEventListener("keydown", (event) => {
    if (event.key === "Escape") close();
  });
</script>

<style>
/* =========================
   NAVBAR
========================= */
.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  position: sticky;
  top: 0;
  left: 0;
  width: 100%;
  height: var(--navbar-height);
  z-index: 1000;
  box-sizing: border-box;
  padding: 0 40px;
  background: var(--color-bg-navbar);
  backdrop-filter: blur(12px);
  -webkit-backdrop-filter: blur(12px);
}

.left {
  color: var(--text-main);
  font-weight: 600;
  font-size: 18px;
}

.right {
  display: flex;
  gap: 24px;
}

a {
  color: var(--text-normal);
  text-decoration: none;
  font-size: 16px;
  position: relative;
  padding-bottom: 4px;
  transition: color var(--transition-speed) var(--ease-in-out), opacity var(--transition-speed) var(--ease-out);
}

a:hover {
  color: var(--text-main);
}

a::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: 0;
  width: 100%;
  height: 1.5px;
  background: var(--color-accent);
  transform: scaleX(0);
  transform-origin: right;
  transition: transform var(--transition-speed) var(--ease-out);
}

a:hover::after {
  transform: scaleX(1);
  transform-origin: left;
}

a:active {
  opacity: 0.7;
}

/* =========================
   HAMBURGER
========================= */
.menu-btn {
  display: none;
  background: none;
  border: none;
  color: var(--text-main);
  font-size: 26px;
  transition: transform var(--transition-speed) var(--ease-out);
}

.menu-btn:active {
  transform: scale(0.9);
}

/* =========================
   MOBILE
========================= */
@media (max-width: 768px), (orientation: portrait) and (max-width: 1024px) {
  .right {
    display: none;
  }

  .menu-btn {
    display: block;
    position: absolute;
    left: 24px;
  }

  .navbar {
    justify-content: center;
  }
}
</style>